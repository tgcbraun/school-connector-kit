{
  "name": "@school-connector-kit/connector-dieschulapp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.buildtime.json"
  },
  "dependencies": {
    "@school-connector-kit/core": "workspace:*"
  },
  "description": "DieSchulApp connector for the school-connector-kit normalized contract (schema 0.1): the `timetable_entry` capability over the current-timetable endpoint."
}
