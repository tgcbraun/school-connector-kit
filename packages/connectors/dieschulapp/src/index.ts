/**
 * Package entry point: the DieSchulApp connector and its configuration
 * type. Named re-exports of the shippable surface only.
 */
export { createDieSchulAppConnector } from "./connector.js";
export type { DieSchulAppConfig } from "./connector.js";
