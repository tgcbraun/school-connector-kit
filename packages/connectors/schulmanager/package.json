{
  "name": "@school-connector-kit/connector-schulmanager",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "description": "Schulmanager connector for the school-connector-kit normalized contract (schema 0.1): the `message` capability over Schulmanager Elternbriefe.",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.buildtime.json"
  },
  "dependencies": {
    "@school-connector-kit/core": "workspace:*"
  }
}
