# Packages

Reusable project packages live here.

- `core/` contains the normalized schema 0.1 and the connector runtime contract (`packages/core/`).
- `connectors/` is empty. No connector implementations exist anywhere in this repository yet.
