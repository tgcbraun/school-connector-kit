# Packages

Reusable project packages live here.

- `core/` contains the normalized schema 0.1 and the connector runtime contract (`packages/core/`).
- `connectors/` contains three connectors: `webuntis/`, `schulmanager/`, and `dieschulapp/`.
- `transport/` contains the host-side Transport that connectors receive (ADR-011).
