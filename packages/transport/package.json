{
  "name": "@school-connector-kit/transport",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.buildtime.json"
  },
  "dependencies": {
    "@school-connector-kit/core": "workspace:*"
  },
  "description": "Host-side Transport implementation of the ADR-003 contract, per ADR-011: the cookie jar is the Transport's, Set-Cookie and Cookie are stripped from the returned headers, and redirects are surfaced rather than followed."
}
