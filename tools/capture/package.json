{
  "name": "@school-connector-kit/capture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Privacy-safe structural capture and redaction tooling.",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "capture": "npm run build && node dist/cli.js",
    "test": "npm run build && vitest run",
    "typecheck": "tsc --noEmit"
  }
}
