{
  "name": "school-connector-kit",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source connector toolkit for German school and Kita platforms.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter ./packages/core build",
    "verify": "pnpm run build && pnpm -r typecheck && pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
